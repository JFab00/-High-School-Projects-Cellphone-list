Raccomando la compilazione e avvio del programma usando il batch build&runProject 
perche' se no tutti i file di tipo class verrano creati dentro la cartella main
(cioe' nella cartella dove si trovano i file .java).

I file sono stati programmati usando netbeans, pero' dopo sono stati rimodificati
in modo da funzionare su qualsiasi altro editor.

Riguardo il NB del compito, il programma si compila con o senza 
eclipse/net beans/jcreator o altro, solo che quando viene compilato crea piu'
di un file class, quindi e' meglio usare il batch creato.

La documentazione JavaDoc la ho fatta, solo che fa prima a leggere i commenti scritti nel programma

*Se vuole la copia di backup del file .csv, si trova nella cartella immagini*